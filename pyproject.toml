[project]
name = "coder-federicoservian"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.3",
]

[dependency-groups]
dev = [
    "ipython>=8.30.0",
]
